import { Text, TouchableOpacity } from "react-native";

export function MMButton() {
    return (
        <TouchableOpacity className="bg-amber-500">
            <Text className="text-brex-blue">MMButton</Text>
        </TouchableOpacity>
    )
}